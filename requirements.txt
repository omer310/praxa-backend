# python >=3.10

# LiveKit Agents Framework — all plugins must be on the same version.
# Pin livekit to match livekit-agents 1.6.0 (which requires livekit==1.1.8).
livekit==1.1.8
livekit-api~=1.1.0
livekit-agents~=1.6.0
livekit-plugins-assemblyai~=1.6.0
livekit-plugins-deepgram~=1.6.0
livekit-plugins-openai~=1.6.0
livekit-plugins-elevenlabs~=1.6.0
livekit-plugins-silero~=1.6.0

# Twilio for phone calls
twilio>=8.10.0

# Supabase for database
supabase>=2.0.0

# HTTP server for webhooks/triggers
fastapi>=0.115.0,<1.0.0
uvicorn>=0.32.0
slowapi>=0.1.9  # Rate limiting

# Background tasks
apscheduler>=3.10.0

# OpenAI SDK (used directly by reply_processor)
openai>=1.0.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.26.0
pydantic>=2.5.0
python-dateutil>=2.8.0

# Shared Praxa core (surface-agnostic tools, context, actions, memory).
# Single source of truth: https://github.com/omer310/praxa-core (pinned to a tag).
# After publishing a new praxa_core release, bump the tag below in both backends.
# Local dev alternative: pip install -e ../praxa_core
praxa_core @ git+https://github.com/omer310/praxa-core.git@v0.3.0
